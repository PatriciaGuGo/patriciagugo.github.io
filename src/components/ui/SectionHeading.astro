---
interface Props {
	eyebrow: string;
	title: string;
}
const { eyebrow, title } = Astro.props;
---

<p class="font-mono text-xs uppercase tracking-[0.16em] text-accent dark:text-accent-hot">{eyebrow}</p>
<h2 class="mt-3 text-3xl md:text-4xl font-bold tracking-tight text-balance">{title}</h2>
